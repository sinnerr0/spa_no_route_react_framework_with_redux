import Layer from './Layer';

export default class Popup extends Layer {
    constructor() {
        super(...arguments);
    }

    render(children) {
        return super.render(children);
    }
}